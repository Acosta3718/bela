{
    "name": "salon/mvc-app",
    "description": "Aplicación MVC para gestión de salon con agenda y finanzas",
    "type": "project",
    "require": {
        "php": "^8.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-autoload-dump": [
            "@php -r \"if (!file_exists('vendor/autoload.php')) { echo 'Autoload generado.'; }\""
        ]
    }
}